{
  "name": "text-extractor",
  "version": "1.0.0",
  "description": "extract text from office products and pdf",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "keywords": [
    "text-extractor"
  ],
  "author": "rohmanahmad",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.7.5",
    "@types/node-cron": "^3.0.11",
    "gts": "^6.0.0",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "mysql2": "^3.11.3",
    "node-cron": "^3.0.3",
    "officeparser": "^4.2.0"
  }
}
